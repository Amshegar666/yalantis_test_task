<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Secret Santa App</title>
    <link href="bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <script src="bootstrap/js/bootstrap.bundle.min.js"></script>
  </head>
  <body>
    <div class="container">
    {{#if shuffled}}
      <div class="row justify-content-md-center mt-5">
        <div class="col col-lg-2"></div>
        <div class="col-md-auto">
          <p class="fs-1 text-center">Unfortunately, the system was shuffled and all the Secret Santas were identified.</p>
          <p class="fs-2 text-center">Registration is no longer available.</p>
        </div>
        <div class="col col-lg-2"></div>
      </div>
      <div class="row justify-content-md-center mt-3">
        <div class="col col-lg-2"></div>
        <div class="col-md-auto">
          <button type="button" class="btn btn-primary" onclick="document.location.href='/signin'">Sign In</button>
        </div>
        <div class="col col-lg-2"></div>
      </div>
    {{else}}
      <div class="row justify-content-md-center mt-5">
        <div class="col col-lg-2"></div>
        <div class="col-md-auto">
          <p class="fs-1 text-center">Hey! Welcome to the "Secret Santa App"</p>
          <p class="fs-2 text-center">Join us before all the places are taken!</p>
          <p class="fs-2 text-center">Current count of free places: <span class="fw-bold">{{count}}</span></p>
        </div>
        <div class="col col-lg-2"></div>
      </div>
      <div class="row justify-content-md-center mt-3">
        <div class="col col-lg-2"></div>
        <div class="col-md-auto">
          <button type="button" class="btn btn-primary" onclick="document.location.href='/signin'">Sign In</button>
          {{#if regPermission}}
            <button type="button" class="btn btn-primary" onclick="document.location.href='/signup'">Sign Up</button>
          {{/if}}
        </div>
        <div class="col col-lg-2"></div>
      </div>
    {{/if}}
    </div>
  </body>
</html>